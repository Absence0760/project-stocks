# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Dependencies
node_modules/
.pnpm-store/
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
.eggs/
*.egg-info/

# Build output
dist/
build/
.svelte-kit/
.next/
.nuxt/
.output/
out/
.turbo/
.parcel-cache/

# Test / coverage
coverage/
.nyc_output/
test-results/
playwright-report/
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/

# Environment / secrets. Real production secrets do NOT live in project repos —
# they live in the private `infra-secrets` repo (see CLAUDE.md "Secrets").
# Committed `.env.development` (non-sensitive local defaults; see CLAUDE.md) is
# NOT matched here, so it stays tracked. Machine-local real values go in the
# gitignored `.env.development.local` (caught by `.env.*.local`).
.env
.env.local
.env.*.local
!.env.example
!.env.*.example

# Guard: no SOPS-encrypted secrets in a project repo (they belong in infra-secrets).
*.sops
*.sops.yaml
!*.sops.yaml.example

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform.lock.hcl.bak
crash.log

# Partial-backend config — embeds account-id in the bucket name, so
# templates that use the bootstrap-aware partial-backend pattern keep
# the per-account value out of git. Bare name matches the file at any
# depth (infra/backend.config, web/infra/backend.config, etc.).
backend.config

# AWS SAM / serverless
.aws-sam/
.serverless/

# Python misc
.python-version

# Sanity
.sanity/

# Local dev infra
.localstack/

# Persona audit reports
# `.claude/agents/persona-*.md` auditors write their findings here. The reports
# are per-clone working notes (often half-reconciled), never a committed
# artifact — but the folder's README documents the convention, so keep that one
# file tracked via the negation below. Glob the contents (`/reviews/*`), not the
# directory (`/reviews/`) — git won't descend into an ignored directory, so a
# directory-level ignore would defeat the negation.
/reviews/*
!/reviews/README.md

# --- Dart / Flutter -------------------------------------------------------
# The workspace root resolves for every member, so .dart_tool/ and the shared
# pubspec.lock live here rather than under apps/mobile.
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
**/ios/Flutter/ephemeral/
**/ios/Pods/
**/ios/.symlinks/
**/android/.gradle/
**/android/local.properties
**/android/key.properties
*.jks
*.keystore

# Claude Code creates agent worktrees here. They are real checkouts of this repo
# and must never be committed into it.
.claude/worktrees/
